import styled from 'styled-components';

export const LayoutRCM = styled.div`
  padding: 50px 20px;
  background-color: '#fff';
  height: ${props => props.parentHeight}px;
  width: 100%;
  overflow-y: scroll;
`;