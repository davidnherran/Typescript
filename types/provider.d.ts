type TWisengineProvider = {
  children: React.ReactNode;
  config: {
    config: {
      token: string;
      theme: "nbd" | "harmony";
      customColor: string;
    };

    token: string;
    theme: "nbd" | "harmony";
    customColor: string;
  };
};
